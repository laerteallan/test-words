
language: python
matrix:
  include:
  - name: "3.10.2 Unittest"
    python: "3.10-dev"
    env: TEST_SUITE=UNITTEST

install:
  - pip install -r requirements-dev.txt

branches:
  only:
  - main

script: 
  - make tests

after_success:
  - coveralls
